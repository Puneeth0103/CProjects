#include<stdio.h>
int sum_digits(long n);

int s;
main()
{
	long n;int sum;
	printf("Enter a number\n");
	scanf("%d",&n);
	sum=sum_digits(n);
	printf("\nThe sum of it's digits: %d",sum);
	return 0;
}

int sum_digits(long n)
{
	s=s+(n%10);
	n=n/10;
	if(n==0)
		return(s);
	else
		sum_digits(n);
}
